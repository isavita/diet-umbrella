<h1>Edit Video</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.video_path(@conn, :update, @video)) %>

<%= link "All Videos", to: Routes.video_path(@conn, :index), role: "button", class: "btn btn-info" %>
